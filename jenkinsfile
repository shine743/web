pipeline {
    agent any

    stages {
        stage('Check Docker Path') {
            steps {
                script {
                    sh 'which docker'
                }
            }
        }
    }
}