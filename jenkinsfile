pipeline {
    agent any

    stages {
        stage('Check Docker Path') {
            steps {
                script {
                    echo 'Checking Docker version..'
                    echo $PATH
                    sh 'bash-c "which docker || echo Docker not found"'
                }
            }
        }
    }
}